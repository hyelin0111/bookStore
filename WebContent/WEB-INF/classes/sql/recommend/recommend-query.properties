selectRecommendList=SELECT R.* FROM( SELECT ROWNUM AS RNUM, R.*,(select book_title from book where book_no = R.book_no and book_status='N') bookTitle FROM ( SELECT * FROM RECOMMEND ) R ) R WHERE RNUM BETWEEN ? AND ?